workflows:
  ios-release:
    name: iOS App Store Direct Upload
    integrations:
      app_store_connect: app_store_connect
    environment:
      xcode: latest
      node: latest
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build Web Assets
        script: npm run build
      - name: Force Create Capacitor iOS & Podfile
        script: |
          npx cap add ios || true
          npx cap sync ios
          if [ ! -f "ios/App/Podfile" ]; then
            cd ios/App && pod init && cd ../..
          fi
      - name: Install CocoaPods
        script: |
          cd ios/App && pod install
      - name: Fetch and Create Signing Certificates / Profiles
        script: |
          app-store-connect fetch-signing-files "binusman.application" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Build Signed Archive
        script: |
          xcodebuild -workspace ios/App/App.xcworkspace \
            -scheme App \
            -config Release \
            -archivePath ios/App/build/App.xcarchive archive
          xcodebuild -exportArchive \
            -archivePath ios/App/build/App.xcarchive \
            -exportPath ios/App/build/ipa \
            -exportOptionsPlist ios/App/App/ExportOptions.plist || true
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: false
    artifacts:
      - ios/App/build/ipa/*.ipa
